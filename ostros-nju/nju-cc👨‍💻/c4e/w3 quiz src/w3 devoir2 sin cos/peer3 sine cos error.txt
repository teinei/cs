#include <stdio.h>
 
#include <math.h>
 
int main()
 
{
       float i = 0.314;// assing values
       float j = 0.25;// assing values
       float sin_value = sin(i);
       float cos_value = cos(i);
 	   printf("The value of sin(%f) : %f \n", i, sin_value);//result
       printf("The value of cos(%f) : %f \n", i, cos_value);//result
       return 0;
}